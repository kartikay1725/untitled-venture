from fastapi import APIRouter, Depends, HTTPException, status\nfrom sqlalchemy.ext.asyncio import AsyncSession\nfrom ..database import async_session\nfrom ..models import MVPBlueprint\nfrom ..services.mvp_generation_service import generate_mvp\nfrom pydantic import BaseModel\nfrom typing import Dict\n\nrouter = APIRouter()\n\nclass MVPResponse(BaseModel):\n    mvp_id: str\n    pdf_url: str\n    download_url: str\n\n@router.post('/', response_model=MVPResponse)\nasync def create_mvp(payload: Dict, user_id: str = Depends()):\n    idea_id = payload.get('idea_id')\n    if not idea_id:\n        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail='idea_id required')\n    blueprint = await generate_mvp(idea_id)\n    return MVPResponse(mvp_id=str(blueprint.id), pdf_url=blueprint.pdf_url, download_url=blueprint.pdf_url)
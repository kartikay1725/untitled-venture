import { test, expect } from '@playwright/test';\n\ntest('idea submission and validation flow', async ({ page }) => {\n  await page.goto('/');\n  await page.fill('textarea', 'A revolutionary platform that uses AI to validate business ideas instantly.');\n  await page.click('button:has-text("Validate Idea")');\n  await expect(page.locator('text=Validation Score')).toBeVisible();\n  await expect(page.locator('text=Generate Blueprint')).toBeVisible();\n});